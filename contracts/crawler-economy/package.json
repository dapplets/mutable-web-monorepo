{
  "name": "@mweb/crawler-economy",
  "scripts": {
    "build": "cargo build --all --target wasm32-unknown-unknown --release",
    "test": "cargo test",
    "deploy": "near contract deploy app.crwl.near use-file ./target/wasm32-unknown-unknown/release/crawler_economy.wasm without-init-call network-config mainnet sign-with-keychain send"
  }
}
